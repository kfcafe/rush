---
id: '5.1'
title: Wire up syntax highlighting in the REPL
slug: wire-up-syntax-highlighting-in-the-repl
status: closed
priority: 2
created_at: '2026-03-12T07:24:28.601592Z'
updated_at: '2026-03-12T07:42:56.484353Z'
closed_at: '2026-03-12T07:42:56.484353Z'
parent: '5'
verify: grep -q 'with_highlighter' src/main.rs && cargo build 2>&1 | tail -1 | grep -q 'Finished'
fail_first: true
checkpoint: '061f5956fc5b614060d3a14408d92eff84e51863'
claimed_by: pi-agent
claimed_at: '2026-03-12T07:29:21.540892Z'
is_archived: true
history:
- attempt: 1
  started_at: '2026-03-12T07:42:56.490276Z'
  finished_at: '2026-03-12T07:44:12.016329Z'
  duration_secs: 75.526
  result: pass
  exit_code: 0
attempt_log:
- num: 1
  outcome: success
  agent: pi-agent
  started_at: '2026-03-12T07:29:21.540892Z'
  finished_at: '2026-03-12T07:42:56.484353Z'
---

## Task
The `RushHighlighter` in `src/highlight.rs` is fully implemented but never connected to the reedline editor. Wire it in.

## Steps
1. In `src/main.rs`, in `run_interactive_with_reedline`, add `.with_highlighter(Box::new(highlight::RushHighlighter::new(builtins.clone())))` to the `Reedline::create()` builder chain.
2. Add `mod highlight;` if it's not already declared in main.rs (check — it is declared in `lib.rs` but main.rs may need the `use` or `mod`).
3. Verify it compiles and the highlighter is constructed with the existing `builtins` Arc.

## Context
```rust
// Current builder in run_interactive_with_reedline (src/main.rs ~L737):
let mut line_editor = Reedline::create()
    .with_completer(completer)
    .with_history(history)
    .with_hinter(hinter);
```

```rust
// src/highlight.rs — already exists:
pub struct RushHighlighter {
    builtins: Arc<Builtins>,
}
impl RushHighlighter {
    pub fn new(builtins: Arc<Builtins>) -> Self { ... }
}
impl Highlighter for RushHighlighter { ... }
```

`builtins` is already an `Arc<Builtins>` in scope at that point.

## Files
- src/main.rs (modify — add `.with_highlighter(...)` to reedline builder)
- src/highlight.rs (read only — already complete)

## Don't
- Don't modify highlight.rs — it's already done
- Don't add new dependencies

## Produces
- syntax-highlighting-wired
